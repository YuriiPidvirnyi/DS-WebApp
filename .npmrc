# Speed up npm install on CI
auto-install-peers=true
fund=false
audit=false
prefer-offline=true
progress=false
loglevel=error

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=false
strict-peer-dependencies=false
