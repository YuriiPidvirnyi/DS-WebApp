# Speed up npm install on CI
auto-install-peers=true
fund=false
audit=false
progress=false
loglevel=error

# Handle optional dependencies (Rollup native bindings)
#Force install on all platforms
optional=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=false
strict-peer-dependencies=false
